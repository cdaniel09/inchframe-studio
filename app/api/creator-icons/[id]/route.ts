import { getChatGPTUser } from '@/app/chatgpt-auth';
import { getCreatorIconForViewer } from '@/lib/data';
import { readStoredFile } from '@/lib/storage';
export const runtime='nodejs';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const user=await getChatGPTUser();const icon=await getCreatorIconForViewer(id,user);if(!icon)return new Response('Not found.',{status:404});if(!icon.objectKey){const initials=icon.displayName.split(/\s+/).map(value=>value[0]).join('').slice(0,2).toUpperCase()||'IF';const svg=`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 256 256"><rect width="256" height="256" rx="52" fill="#111411"/><circle cx="128" cy="128" r="92" fill="#1d3212" stroke="#9cf21f" stroke-width="4"/><text x="128" y="145" text-anchor="middle" font-family="Arial,sans-serif" font-size="64" font-weight="900" fill="#9cf21f">${initials}</text></svg>`;return new Response(svg,{headers:{'content-type':'image/svg+xml','cache-control':icon.public?'public, max-age=3600':'private, max-age=120','x-content-type-options':'nosniff'}});}try{const value=await readStoredFile(icon.objectKey);return new Response(new Uint8Array(value),{headers:{'content-type':icon.mimeType,'content-length':String(value.byteLength),'cache-control':icon.public?'public, max-age=3600':'private, max-age=120','x-content-type-options':'nosniff'}});}catch{return new Response('Not found.',{status:404});}}
