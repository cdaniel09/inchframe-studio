import { env } from 'cloudflare:workers';
import { getChatGPTUser } from '@/app/chatgpt-auth';
import { getAssetForUser } from '@/lib/data';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const user=await getChatGPTUser();if(!user)return new Response('Sign in required.',{status:401});const{id}=await params;const asset=await getAssetForUser(id,user);if(!asset)return new Response('Not found.',{status:404});const object=await env.FILES.get(asset.object_key);if(!object)return new Response('Not found.',{status:404});const filename=asset.filename.replace(/["\r\n]/g,'_');return new Response(object.body,{headers:{'content-type':asset.mime_type,'content-length':String(asset.byte_size),'content-disposition':`inline; filename="${filename}"`,'cache-control':'private, max-age=300','x-content-type-options':'nosniff'}});}
